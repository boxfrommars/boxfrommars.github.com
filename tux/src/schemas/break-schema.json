{
    "id": "/break",
    "title": "Break Schema",
    "type": "object",
    "properties": {
        "type": {
            "type": "string",
            "enum": ["rest", "unplanned", "lunch"]
        },
        "startTime": {
            "type": "integer"
        }
    },
    "required": ["type", "startTime"]
}